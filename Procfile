# Procfile für Railway/Render/Heroku Deployment

# Telegram Bot als Worker-Prozess
worker: python telegram_bot/bot.py

# Optional: Streamlit Web-Interface (wenn beide laufen sollen)
# web: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
